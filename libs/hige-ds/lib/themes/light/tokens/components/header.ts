import { alias } from '../alias';

const { newColors } = alias;

export const headerTokens = {
  color: newColors.textInverse,
  backgroundColor: newColors.backgroundBrand,
  borderColor: newColors.borderLight,
};
