import { alias } from '../alias';

export const drawerTokens = {
  color: alias.newColors.textPrimary,
  title: {
    color: alias.newColors.textPrimary,
  },
  subtitle: {
    color: alias.newColors.textPrimary,
  },
  description: {
    color: alias.newColors.textPrimary,
  },
  leftContentBackground: alias.newColors.canvas,
  rightContentBackground: alias.newColors.background,
};
